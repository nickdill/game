# How to use the web build
Can run server with `npx serve`, go to localhost:3000, or use any other webserver

Export default puts the web build here.
Export new builds and commit to github to release
Currently github pages deploys the `beta` branch


